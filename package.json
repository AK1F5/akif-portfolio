{
  "name": "akif-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling to lint and bundle the Akif portfolio site.",
  "scripts": {
    "lint": "akif-lint",
    "lint:html": "akif-lint --scope html",
    "lint:css": "akif-lint --scope css",
    "lint:js": "akif-lint --scope js",
    "test": "npm run lint",
    "build": "akif-build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@akif-portfolio/tooling": "file:./scripts"
  }
}
