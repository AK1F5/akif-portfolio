{
  "name": "akif-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling to lint and bundle the Akif portfolio site.",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "lint:html": "node scripts/lint.mjs --scope html",
    "lint:css": "node scripts/lint.mjs --scope css",
    "lint:js": "node scripts/lint.mjs --scope js",
    "test": "npm run lint",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
